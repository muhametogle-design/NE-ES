[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
# -ra: show summary of all non-passing tests; --strict-markers: fail on
# unregistered markers (typos in @pytest.mark.* become errors, not silent no-ops).
addopts = -ra --strict-markers
