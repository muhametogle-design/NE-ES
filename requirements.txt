# =============================================================================
# NE-EMIS — Backend (FastAPI) dependencies
# Pin minor versions for reproducible container builds.
# =============================================================================
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # OAuth2 password form parsing
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                   # pinned: passlib 1.7.4 reads bcrypt.__about__ (removed in bcrypt>=4.1)
python-jose[cryptography]==3.3.0
email-validator==2.2.0
psycopg[binary]==3.2.3          # PostgreSQL driver (postgresql+psycopg://)
alembic==1.14.0                 # database migrations
slowapi==0.1.9                  # rate limiting (Starlette/FastAPI)
python-dotenv==1.0.1
