Generic single-database configuration for NE-EMIS (Alembic).

- `env.py` loads `Base.metadata` from `app.models.base` and imports the
  `app.models` package (tenancy, academic, finance, compliance, backups,
  biometrics, absence, syllabus) so every table is registered.
- The database URL is resolved from `NE_EMIS_DATABASE_URL`/`DATABASE_URL`
  (shell environment, then the repo-root `.env` loaded via python-dotenv)
  or `app.core.config.settings` — never hard-code it here.

Common commands:
    alembic upgrade head        # apply all migrations
    alembic revision --autogenerate -m "describe change"
    alembic downgrade -1        # roll back one migration
    alembic current             # show current revision
    alembic history             # migration history
